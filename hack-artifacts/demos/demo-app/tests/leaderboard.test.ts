import { describe, it, expect } from 'vitest';
// import { leaderboardService } from '../src/services/leaderboardService';

// TODO: Write tests for the leaderboard service
//
// Test cases to consider:
// - getLeaderboard() returns entries sorted by win rate
// - getTopFighters(3) returns exactly 3 entries
// - Fighters with no battles are excluded or ranked last
// - Win rate calculation is correct (wins / total battles)
// - Streak tracking works correctly
//
// Note: You'll need to set up test data using the fighterService

describe('LeaderboardService', () => {
  it.todo('getLeaderboard() returns entries sorted by win rate');
  it.todo('getTopFighters(3) returns exactly 3 entries');
  it.todo('fighters with no battles are excluded or ranked last');
  it.todo('win rate calculation is correct');
  it.todo('streak tracking works correctly');
});
